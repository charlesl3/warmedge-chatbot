name: Weekly Topic Distillation

on:
  schedule:
    - cron: '0 8 * * 0'

  workflow_dispatch:

jobs:
  distill:
    runs-on: ubuntu-latest

    steps:
      - name: Call WarmGPT Distill Endpoint
        run: |
          curl -X POST https://warmedge-chatbot-production.up.railway.app/internal/topics/distill-all
